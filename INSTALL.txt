PM2B Pack 09 correction

Install with PowerShell:

Expand-Archive `
  -Path "$env:USERPROFILE\Downloads\pm2b-pack-09-correction.zip" `
  -DestinationPath "C:\Users\Clark\Projects\kingshot-text-lab" `
  -Force

This replaces the three Supabase repository files with syntax compatible with erasableSyntaxOnly.

Then run:
npm run build
npm run lint
