import { useEffect, useMemo, useState } from 'react'
import { listCommunityArtQueue, moderateCommunityArt, type CommunityArtCompatibility, type CommunityArtRecord } from '../../services/communityArtService'

type QueueFilter = 'all' | 'pending' | 'approved'

export function CommunityArtModerationPage() {
  const [queue, setQueue] = useState<CommunityArtRecord[]>([])
  const [filter, setFilter] = useState<QueueFilter>('all')
  const [category, setCategory] = useState('All')
  const [loading, setLoading] = useState(true)
  const [error, setError] = useState<string | null>(null)
  const [notice, setNotice] = useState('')
  const [working, setWorking] = useState<string | null>(null)
  const [notes, setNotes] = useState<Record<string, string>>({})
  const [feedback, setFeedback] = useState<Record<string, string>>({})

  useEffect(() => { void listCommunityArtQueue().then(setQueue).catch(() => setError('The moderation queue could not be loaded.')).finally(() => setLoading(false)) }, [])
  const categories = useMemo(() => ['All', ...new Set(queue.map((item) => item.category))], [queue])
  const visibleQueue = useMemo(() => queue.filter((item) => (filter === 'all' || item.status === filter) && (category === 'All' || item.category === category)), [queue, filter, category])
  async function act(item: CommunityArtRecord, action: 'approve' | 'reject' | 'publish') {
    const note = notes[item.id] ?? ''
    if (action === 'reject' && !note.trim()) { setError('Add a private moderation reason before rejecting artwork.'); return }
    setError(null); setNotice(''); setWorking(item.id)
    try { const updated = await moderateCommunityArt({ id: item.id, action, note, feedback: feedback[item.id] ?? '', compatibilityStatus: item.compatibilityStatus }); setQueue((current) => action === 'approve' ? current.map((entry) => entry.id === item.id ? updated : entry) : current.filter((entry) => entry.id !== item.id)); setNotice(`${item.title} ${action === 'approve' ? 'approved' : action === 'reject' ? 'rejected' : 'published'}.`) } catch (caught) { setError(caught instanceof Error ? caught.message : 'Moderation action failed.') } finally { setWorking(null) }
  }
  async function updateCompatibility(item: CommunityArtRecord, compatibilityStatus: CommunityArtCompatibility) { setError(null); setWorking(item.id); try { const updated = await moderateCommunityArt({ id: item.id, action: 'update', note: notes[item.id] ?? '', feedback: feedback[item.id] ?? '', compatibilityStatus }); setQueue((current) => current.map((entry) => entry.id === item.id ? { ...entry, compatibilityStatus: updated.compatibilityStatus } : entry)); setNotice('Compatibility status saved.') } catch (caught) { setError(caught instanceof Error ? caught.message : 'Compatibility update failed.') } finally { setWorking(null) } }
  async function copyArtwork(text: string) { try { await navigator.clipboard.writeText(text); setNotice('Artwork copied exactly.') } catch { setError('Copy failed. Select the artwork manually and copy it.') } }
  return <main className="admin-page community-art-admin-page"><section className="admin-page__header"><p className="admin-page__eyebrow">Community Art Studio</p><h1>Artwork moderation</h1><p>Review the exact text players will paste into Kingshot, verify attribution and publish only approved work.</p></section>{error && <div className="error-state" role="alert">{error}</div>}{notice && <div className="success-state" role="status">{notice}</div>}<div className="community-art-admin-summary"><strong>{queue.filter((item) => item.status === 'pending').length} pending</strong><strong>{queue.filter((item) => item.status === 'approved').length} approved</strong><strong>{queue.length} in queue</strong></div><div className="community-art-admin-filters"><label>Status<select value={filter} onChange={(event) => setFilter(event.target.value as QueueFilter)}><option value="all">All actionable</option><option value="pending">Pending review</option><option value="approved">Approved to publish</option></select></label><label>Category<select value={category} onChange={(event) => setCategory(event.target.value)}>{categories.map((item) => <option key={item}>{item}</option>)}</select></label></div>{loading && <div className="loading-state" role="status">Loading moderation queue…</div>}{!loading && visibleQueue.length === 0 && <div className="empty-state"><h2>Queue clear</h2><p>No artwork matches this filter.</p></div>}<div className="community-art-moderation-grid">{visibleQueue.map((item) => <article className="community-art-moderation-card" key={item.id}><div><p className="eyebrow">{item.category} · {item.status}</p><h2>{item.title}</h2><p>{item.description || 'No description provided.'}</p><p className="community-art-moderation-meta"><strong>Submitter:</strong> {item.submitterContext?.userId ?? 'Account unavailable'}<br /><strong>Attribution:</strong> {item.attribution || 'Anonymous'} ({item.submitterContext?.attributionType ?? 'anonymous'})<br />Submitted {new Date(item.createdAt).toLocaleString()}</p><pre className="community-art-moderation-preview">{item.artworkText}</pre><p>{item.characterCount} characters · {item.lineCount} lines · {item.sizeClass}</p><label>Compatibility<select value={item.compatibilityStatus} disabled={working === item.id} onChange={(event) => void updateCompatibility(item, event.target.value as CommunityArtCompatibility)}><option value="untested">Untested</option><option value="needs_testing">Needs testing</option><option value="verified">Verified</option><option value="known_issues">Known issues</option></select></label><label>Private moderation note<textarea value={notes[item.id] ?? ''} maxLength={4000} onChange={(event) => setNotes((current) => ({ ...current, [item.id]: event.target.value }))} placeholder="Required when rejecting" /></label><label>Submitter feedback<textarea value={feedback[item.id] ?? ''} maxLength={2000} onChange={(event) => setFeedback((current) => ({ ...current, [item.id]: event.target.value }))} placeholder="Optional feedback" /></label><div className="community-art-moderation-actions"><button type="button" className="button button--secondary" disabled={working === item.id} onClick={() => void copyArtwork(item.artworkText)}>Copy artwork</button>{item.status === 'pending' && <><button type="button" className="button button--primary" disabled={working === item.id} onClick={() => void act(item, 'approve')}>{working === item.id ? 'Saving…' : 'Approve'}</button><button type="button" className="button button--secondary" disabled={working === item.id} onClick={() => void act(item, 'reject')}>Reject</button></>}{item.status === 'approved' && <button type="button" className="button button--primary" disabled={working === item.id} onClick={() => void act(item, 'publish')}>Publish</button>}</div></div></article>)}</div></main>
}
