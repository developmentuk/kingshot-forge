/**
 * Compatibility export for the Player Identity validation surface.
 * Public player rendering is owned by PlayerProfilePage so the application
 * has one real public projection instead of a synthetic identity preview.
 */
export { default as PublicPlayerIdentityPage } from '../../pages/PlayerProfilePage'
