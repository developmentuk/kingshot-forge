import { useEffect, useState } from 'react'
import { Link, useNavigate, useParams } from 'react-router-dom'
import { getUser, getUsers, mutateUser, type UserListQuery, type UserListResponse } from '../../services/userManagementService'
import type { UserDetail } from '../../../server/identity/contracts'

const emptyList: UserListResponse = { items: [], page: 1, pageSize: 20, total: 0, totalPages: 0 }
function StatusBadge({ value }: { value: string }) { return <span className="status-badge">{value.replaceAll('_', ' ')}</span> }

export function UserManagementPage() {
  const [query, setQuery] = useState<UserListQuery>({ page: 1, pageSize: 20 })
  const [result, setResult] = useState<UserListResponse>(emptyList)
  const [loading, setLoading] = useState(true)
  const [error, setError] = useState('')
  useEffect(() => { const timer = window.setTimeout(() => { setLoading(true); setError(''); void getUsers(query).then(setResult).catch((reason: unknown) => setError(reason instanceof Error ? reason.message : 'User Management is unavailable.')).finally(() => setLoading(false)) }, query.search ? 300 : 0); return () => window.clearTimeout(timer) }, [query])
  return <main className="operations-users"><header className="operations-users__header"><div><p className="eyebrow">Player Operations</p><h1>User Management</h1><p>Safe Forge Identity projections for authorized Operations staff.</p></div><span className="status-badge">Server paginated</span></header><section className="operations-users__filters" aria-label="User filters"><label>Search<input value={query.search ?? ''} onChange={(event) => setQuery({ ...query, search: event.target.value, page: 1 })} placeholder="Name, masked email, kingdom or ID" /></label><label>Role<select value={query.role ?? ''} onChange={(event) => setQuery({ ...query, role: event.target.value || undefined, page: 1 })}><option value="">All roles</option><option value="owner">Owner</option><option value="admin">Administrator</option><option value="moderator">Moderator</option><option value="content_creator">Content Creator</option><option value="contributor">Contributor</option><option value="viewer">Player</option></select></label><label>Status<select value={query.status ?? ''} onChange={(event) => setQuery({ ...query, status: event.target.value || undefined, page: 1 })}><option value="">All statuses</option><option value="active">Active</option><option value="restricted">Restricted</option><option value="suspended">Suspended</option><option value="deactivated">Deactivated</option></select></label></section>{error && <div className="error-state" role="alert">{error}</div>}{loading && <div className="loading-state" role="status">Loading Forge identities…</div>}{!loading && !error && result.items.length === 0 && <div className="empty-state"><h2>No Forge identities found</h2><p>Try a broader search or remove a filter.</p></div>}{!loading && !error && result.items.length > 0 && <><div className="operations-users__table-wrap"><table><caption className="sr-only">Forge identities</caption><thead><tr><th scope="col">Identity</th><th scope="col">Roles</th><th scope="col">Status</th><th scope="col">Player</th><th scope="col">Kingdom</th><th scope="col">Consent</th><th scope="col"><span className="sr-only">Actions</span></th></tr></thead><tbody>{result.items.map((user) => <tr key={user.userId}><th scope="row"><Link to={`/operations/users/${user.userId}`}>{user.displayName}</Link><small>{user.safeEmail ?? 'Email masked'}</small></th><td>{user.roles.map((role) => <StatusBadge key={role} value={role} />)}</td><td><StatusBadge value={user.accountStatus} /></td><td>{user.linkedPlayerCount ? `${user.linkedPlayerCount} linked` : 'Unlinked'}</td><td>{user.kingdom ?? '—'}</td><td>{user.autoRedeemConsent}</td><td><Link className="button button--secondary" to={`/operations/users/${user.userId}`}>Open</Link></td></tr>)}</tbody></table></div><nav className="operations-users__pagination" aria-label="User list pagination"><button type="button" disabled={result.page <= 1} onClick={() => setQuery({ ...query, page: result.page - 1 })}>Previous</button><span>Page {result.page} of {Math.max(1, result.totalPages)} · {result.total} identities</span><button type="button" disabled={result.page >= result.totalPages} onClick={() => setQuery({ ...query, page: result.page + 1 })}>Next</button></nav></>}</main>
}

export function UserDetailPage() {
  const { userId = '' } = useParams(); const navigate = useNavigate(); const [user, setUser] = useState<UserDetail | null>(null); const [error, setError] = useState(''); const [working, setWorking] = useState(false); const [reason, setReason] = useState(''); const [role, setRole] = useState('contributor'); const [status, setStatus] = useState('active')
  useEffect(() => { void getUser(userId).then((value) => { setUser(value); setStatus(value.accountStatus) }).catch((reasonValue: unknown) => setError(reasonValue instanceof Error ? reasonValue.message : 'User detail is unavailable.')) }, [userId])
  async function mutate(action: 'assign_role' | 'revoke_role' | 'change_status') { setWorking(true); setError(''); try { await mutateUser(userId, { action, role: action !== 'change_status' ? role : undefined, status: action === 'change_status' ? status : undefined, reason }); setReason(''); setUser(await getUser(userId)) } catch (reasonValue) { setError(reasonValue instanceof Error ? reasonValue.message : 'The identity mutation failed.') } finally { setWorking(false) } }
  if (error && !user) return <main className="workspace-status"><p className="eyebrow">Forge Identity</p><h1>User detail unavailable</h1><p role="alert">{error}</p><Link className="button button--secondary" to="/operations/users">Return to User Management</Link></main>
  if (!user) return <main className="workspace-status"><p className="eyebrow">Forge Identity</p><h1>Loading user detail…</h1></main>
  return <main className="operations-user-detail"><header className="operations-user-detail__header"><div><Link to="/operations/users">← User Management</Link><p className="eyebrow">Forge Identity</p><h1>{user.displayName}</h1><p>{user.safeEmail ?? 'Email masked'} · joined {new Date(user.createdAt).toLocaleDateString()}</p></div><StatusBadge value={user.accountStatus} /></header>{error && <div className="error-state" role="alert">{error}</div>}<div className="operations-user-detail__grid"><section><h2>Overview</h2><dl><div><dt>Account status</dt><dd><StatusBadge value={user.accountStatus} /></dd></div><div><dt>Roles</dt><dd>{user.roles.map((item) => <StatusBadge key={item} value={item} />)}</dd></div><div><dt>Workspaces</dt><dd>{user.workspaces.join(', ')}</dd></div><div><dt>Last sign-in</dt><dd>{user.lastSignInAt ? new Date(user.lastSignInAt).toLocaleString() : 'Unavailable'}</dd></div></dl></section><section><h2>Player Accounts</h2>{user.linkedPlayers.length === 0 ? <p>No linked Player Accounts.</p> : <ul>{user.linkedPlayers.map((player) => <li key={player.playerAccountId}><strong>{player.playerName}</strong> · {player.maskedPlayerId} · Kingdom {player.kingdom} · {player.verificationStatus}</li>)}</ul>}</section><section><h2>Roles and Permissions</h2><p>{user.capabilities.length ? user.capabilities.join(', ') : 'No internal capabilities.'}</p><div className="operations-user-detail__actions"><label>Role<select value={role} onChange={(event) => setRole(event.target.value)}><option value="viewer">Player</option><option value="contributor">Contributor</option><option value="content_creator">Content Creator</option><option value="moderator">Moderator</option><option value="admin">Administrator</option><option value="owner">Owner</option></select></label><button type="button" disabled={working || !reason.trim()} onClick={() => void mutate('assign_role')}>Assign role</button><button type="button" className="button--danger" disabled={working || !reason.trim()} onClick={() => void mutate('revoke_role')}>Revoke role</button></div></section><section><h2>Account status</h2><p>Status changes require a reason and are protected against self-lockout and final-Owner changes.</p><div className="operations-user-detail__actions"><label>Status<select value={status} onChange={(event) => setStatus(event.target.value)}><option value="active">Active</option><option value="restricted">Restricted</option><option value="suspended">Suspended</option><option value="deactivated">Deactivated</option></select></label><button type="button" disabled={working || !reason.trim()} onClick={() => void mutate('change_status')}>Save status</button></div></section><section><h2>Auto Redeem</h2><p>Consent: <strong>{user.autoRedeemConsent}</strong>. Provider payloads and private redemption details are never shown here.</p></section><section><h2>Audit History</h2>{user.audit.length === 0 ? <p>No identity audit events.</p> : <ol>{user.audit.map((entry) => <li key={entry.id}><strong>{entry.action}</strong> · {entry.reason} · {new Date(entry.createdAt).toLocaleString()}</li>)}</ol>}</section></div><label className="operations-user-detail__reason">Mutation reason<textarea value={reason} onChange={(event) => setReason(event.target.value)} minLength={3} maxLength={2000} placeholder="Required for every role or status mutation" /></label><button type="button" className="button button--secondary" onClick={() => navigate('/operations/users')}>Back to users</button></main>
}
