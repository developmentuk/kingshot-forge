import { Link, useParams } from 'react-router-dom'
import { contributorRoles, getContributorRole } from '../data/contributorRoles'

const statusLabel = { open: 'Open for interest', planned: 'Planned' } as const

function Disclosure() {
  return <p className="join-forge__disclosure" role="note"><strong>Unpaid and voluntary:</strong> Forge Contributors are volunteers in a flexible hobby/community project. There is no employment relationship, payment, guaranteed hours or guaranteed acceptance.</p>
}

export function JoinForgePage() {
  return <main className="join-forge"><header className="join-forge__hero"><p className="eyebrow">Community programme</p><h1>Join the Forge</h1><p>Help build, test and support Kingshot Forge.</p><Disclosure /></header><section className="join-forge__intro"><h2>Make a flexible contribution</h2><p>Kingshot Forge is an unofficial community project. Specialist and non-technical opportunities are welcome. Interest is reviewed, access is granted gradually, and every contributor follows the project’s conduct and security expectations.</p><div className="join-forge__principles"><span>Remote and asynchronous</span><span>Small tasks welcome</span><span>Reviewed access</span><span>Community-run</span></div></section><section aria-labelledby="roles-heading"><div className="join-forge__section-heading"><div><p className="eyebrow">Role catalogue</p><h2 id="roles-heading">Find a contribution path</h2></div><span className="status-badge">{contributorRoles.length} roles</span></div><div className="join-forge__grid">{contributorRoles.map((role) => <Link className={`join-role-card join-role-card--${role.status}`} to={`/join/${role.slug}`} key={role.slug}><div className="join-role-card__top"><span className="join-role-card__category">{role.category}</span><span className="status-badge">{statusLabel[role.status]}</span></div><h3>{role.title}</h3><p>{role.summary}</p><footer><span>{role.technical ? 'Technical' : 'Non-technical'}</span><span>Flexible contribution →</span></footer></Link>)}</div></section><section className="join-forge__process"><h2>How it works</h2><ol><li>Explore a role and its expectations.</li><li>Sign in to save and submit an application.</li><li>Applications are reviewed by an authorised Forge operator.</li><li>Accepted contributors begin with a scoped onboarding path.</li><li>Access grows only when the work and trust requirements support it.</li></ol><p>Applications are stored securely and are visible only to the applicant and authorised recruitment reviewers. No application data is collected on this public catalogue.</p></section><section className="join-forge__faq"><h2>Frequently asked questions</h2><details><summary>Are roles paid or employment?</summary><p>No. These are unpaid, voluntary hobby/community roles and do not create an employment relationship.</p></details><details><summary>How much time is required?</summary><p>There are no fixed hours or guaranteed minimum. Reliability and communication are more important than a particular weekly commitment.</p></details><details><summary>Do I need professional experience?</summary><p>No. Several roles need no programming experience, and practical evidence is more useful than a formal title.</p></details><details><summary>Will I receive production access?</summary><p>No. Access is progressive, scoped and capability-based. Production credentials and secrets are never granted automatically.</p></details><details><summary>Can I stop contributing?</summary><p>Yes. Contributors can step away. Project access may also be removed when safety, conduct or security requires it.</p></details></section></main>
}

export function ContributorRolePage() {
  const { roleSlug } = useParams()
  const role = roleSlug ? getContributorRole(roleSlug) : undefined
  if (!role) return <main className="join-forge join-forge--not-found"><p className="eyebrow">Join the Forge</p><h1>Role not found</h1><p>That role is not in the current catalogue.</p><Link className="button button--primary" to="/join">Browse roles</Link></main>
  return <main className="join-forge join-forge--detail"><Link className="join-forge__back" to="/join">← All contributor roles</Link><header className="join-forge__hero"><p className="eyebrow">{role.category} · {role.technical ? 'Technical' : 'Non-technical'}</p><h1>{role.title}</h1><p>{role.summary}</p><Disclosure /><div className="join-forge__detail-status"><span className="status-badge">{statusLabel[role.status]}</span><span>{role.commitment}</span></div></header><div className="join-forge__detail-grid"><section><h2>Role purpose</h2><p>{role.purpose}</p><h2>Typical activities</h2><ul>{role.activities.map((item) => <li key={item}>{item}</li>)}</ul><h2>Useful skills</h2><ul>{role.usefulSkills.map((item) => <li key={item}>{item}</li>)}</ul><h2>Experience</h2><p>{role.experience}</p></section><aside><section><h2>Access and safety</h2><p>{role.access}</p><p>{role.considerations}</p></section><section><h2>Application questions</h2><ul>{role.questions.map((item) => <li key={item}>{item}</li>)}</ul></section><section className="join-forge__cta"><h2>Apply for this path</h2><p>Applications are reviewed. Acceptance does not automatically grant privileged access.</p>{role.status === 'open' ? <Link className="button button--primary" to={`/join/apply?role=${role.slug}`}>Start application</Link> : <p>This role is planned and cannot accept applications yet.</p>}</section></aside></div></main>
}
