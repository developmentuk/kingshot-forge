# Release 0.9.0 RC2 — Selective Platform Integration

This branch selectively integrates the preserved RC2 platform work onto canonical `release/0.9.0-rc` at `9f8d196`, preserving the newer Render Engine, Calibration Lab, Player, Gift and Community Art systems.

Integrated in the first milestone:

- Search contracts, ranking engine, projection builder, persistence boundary, cache, refresh and diagnostics;
- public `/search` and `/api/search` surfaces;
- role-gated `/admin/search` and Search Explorer API;
- Hero Companion Forge Connections over published relationship projections;
- post-publication Search cache invalidation;
- focused Search validation and a local, unapplied Search persistence migration.

Hero Skills’ preserved vertical slice and atomic snapshot migration remain deferred pending schema, canonical Hero Domain, feature-flag, RLS and workflow compatibility review. Snapshot deletions and wholesale shell replacements were excluded.

RC2 is locally buildable and focused Search checks pass. Full readiness still requires the final repository-wide validation and Clark’s authenticated preview/runtime checks. No Supabase migration was applied, and nothing was pushed or deployed by this integration.
